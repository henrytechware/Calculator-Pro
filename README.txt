Calculator Pro PWA

Host this folder on HTTPS (or localhost for testing), then open it in Chrome on Android and choose Install app / Add to Home screen.

PWA files:
manifest.json - app metadata and install configuration
sw.js - offline cache/service worker
icons/icon.svg - app icon
